Test:
    method: "GET"
    url: "http://localhost"
    before: |
        let @a = 3
        import $test
        if $v == 4
            make sense
        some other stuff
    after:
        - let aaa
        -    some other thing
    test: '2018-06-07'

Auth $user:
    method: "POST"
    url: "http://localhost"
    headers:
        content-type: "application/json"
    cookies:
        XDEBUG_SESSIONS: "max_office_kernel"
    after:
        - assert $response.code == 201 || $rrrr == 5
        - print $response.body